---
description: Git workflow for the 7Oz Espresso Cafe monorepo — branching, commits, reviews, and release safety.
alwaysApply: true
---

# Git Workflow

This project follows a professional Git workflow.

Before making any code changes:

1. Always synchronize the latest `develop` branch.

```bash
git checkout develop
git pull origin develop
```

2. Create a new branch from `develop`.

Branch naming convention:

- feature/<feature-name>
- fix/<bug-name>
- hotfix/<critical-fix>
- refactor/<module-name>
- chore/<task-name>
- docs/<documentation-name>

Examples:

```
feature/reservation-system
feature/customer-membership
feature/landing-page
feature/admin-dashboard
fix/login-validation
refactor/menu-service
docs/api-specification
```

3. Branch model:

- `main` — production-ready releases only
- `develop` — integration branch (feeds staging)
- Never use `master` for new work

Never work directly on:

- develop
- main

Release / environment promotion:

- `develop` = integration branch and staging source of truth
- `main` = production-only releases
- When local work on a feature branch is complete, **automatically commit and open a PR into `develop`**. Do not ask whether to commit or open the PR.
- Mistakes are caught in PR review or can be rolled back after merge — that is intentional.
- Do **not** merge the PR into `develop` yourself unless the product owner explicitly asks.
- Do **not** deploy staging until the product owner confirms the PR is merged into `develop`.
- After staging deploy, wait for the product owner to confirm staging is OK.
- The product owner opens the PR from `develop` → `main`. Do not open that release PR unless asked.
- Deploy production only when instructed after the release is on `main`.
- For large cutovers, follow `docs/deployment/PRODUCTION_CUTOVER.md`

4. Make small and meaningful commits.

One completed feature or logical change should equal one commit.

Avoid giant commits containing unrelated changes.

5. Follow Conventional Commits.

Examples:

```
feat: add reservation booking flow

feat: implement loyalty point calculation

fix: resolve navbar responsive issue

refactor: simplify reservation service

docs: update API documentation

style: improve menu card spacing

test: add reservation unit tests

chore: update dependencies
```

6. Before every commit ensure:

- Project builds successfully.
- ESLint passes.
- TypeScript has zero errors.
- Tests pass (if applicable).
- No unused imports.
- No console.log left behind.
- No TODO comments unless intentionally documented.

7. Never push directly to:

- develop
- main

Push only to your working branch.

Example:

```bash
git push -u origin feature/reservation-system
```

8. Open a PR into `develop` automatically when the change is ready. The product owner reviews and merges on GitHub. Do not wait for a chat prompt to create the commit or the PR.

9. Never rewrite public Git history unless explicitly instructed.

10. Keep commit history clean, readable, and professional.

Think of the Git history as permanent project documentation.