# API Documentation

OpenAPI specifications for the 7Oz Espresso Cafe backend.

The runtime OpenAPI shell is currently embedded and served by the API at:

- `/openapi.yaml`
- `/api/v1/openapi.yaml`

Canonical draft for Phase 2 health endpoints lives in the backend server package and will be expanded as modules are added.
